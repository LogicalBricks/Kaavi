%h1 Listado de Significados

- if @significados.any?
  %table
    %thead
      %tr
        %th
        %th
        %th

    %tbody
      - @significados.each do |significado|
        %tr
          %td= link_to 'Mostrar', significado
          %td= link_to 'Editar', edit_significado_path(significado)
          %td= link_to 'Eliminar', significado, method: :delete, data: { confirm: '¿Estás seguro?' }
- else
  %div
    %h3 No se encontraron Significados

%br

= link_to "Nuevo Significado", new_significado_path
