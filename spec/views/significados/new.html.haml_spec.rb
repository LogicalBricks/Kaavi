require 'spec_helper'

describe "significados/new" do
  before(:each) do
    assign(:significado, stub_model(Significado).as_new_record)
  end

  it "renders new significado form" do
    pending "Falta definir la forma en que se probara"
  end

end
